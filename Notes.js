/*
   we are using npx parcel index.html to run the server.

   - we can change the script for this from package.json in 
    "scripts": {
                "start": "parcel index.html",
                "build":"parcel build index.html",
                "test": "jest"
            }
    now we need to run just `npm run start` or npm start work same.
    npm build start // ready for production.




const jsxHeading = <h1>React course using JSX 🚀</h1> 
    - this is not understandable for javascript engine, parsel is working behind this.
    - it is transpiled before going to js engine by parcel using Babel .



















*/